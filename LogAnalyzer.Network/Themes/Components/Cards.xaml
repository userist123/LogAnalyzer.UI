<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Obsidian Forensic Card Style -->
    <Style x:Key="ObsidianCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Surface2Brush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="14"/>
    </Style>

    <!-- GlassCardStyle for Full Backward-Compatibility -->
    <Style x:Key="GlassCardStyle" TargetType="Border" BasedOn="{StaticResource ObsidianCardStyle}"/>

    <!-- Elevated Diagnostic Panel -->
    <Style x:Key="ElevatedPanelStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource ElevatedPanelBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="18"/>
    </Style>

</ResourceDictionary>