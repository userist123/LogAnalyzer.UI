<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===================== PALETA DE CULORI ===================== -->
    <Color x:Key="ColorBgDeep">#FF0A0E14</Color>
    <Color x:Key="ColorBgCard">#CC12161F</Color>
    <Color x:Key="ColorAccent">#FF3B82F6</Color>
    <Color x:Key="ColorAccentLight">#FF60A5FA</Color>
    <Color x:Key="ColorAccentGlow">#553B82F6</Color>
    <Color x:Key="ColorBorder">#3330363D</Color>
    <Color x:Key="ColorTextPrimary">#FFE6EDF3</Color>
    <Color x:Key="ColorTextSecondary">#FF8B949E</Color>
    <Color x:Key="ColorSuccess">#FF2EA043</Color>
    <Color x:Key="ColorDanger">#FFF85149</Color>

    <SolidColorBrush x:Key="BgDeepBrush" Color="{StaticResource ColorBgDeep}"/>
    <SolidColorBrush x:Key="BgCardBrush" Color="{StaticResource ColorBgCard}"/>
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource ColorAccent}"/>
    <SolidColorBrush x:Key="AccentLightBrush" Color="{StaticResource ColorAccentLight}"/>
    <SolidColorBrush x:Key="AccentGlowBrush" Color="{StaticResource ColorAccentGlow}"/>
    <SolidColorBrush x:Key="BorderSoftBrush" Color="{StaticResource ColorBorder}"/>
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource ColorTextPrimary}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource ColorTextSecondary}"/>

    <LinearGradientBrush x:Key="AccentGradientBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{StaticResource ColorAccent}" Offset="0"/>
        <GradientStop Color="{StaticResource ColorAccentLight}" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="WindowBgGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#FF0A0E14" Offset="0"/>
        <GradientStop Color="#FF10151D" Offset="0.5"/>
        <GradientStop Color="#FF0A0E14" Offset="1"/>
    </LinearGradientBrush>

    <!-- ===================== ANIMATIE FADE-IN FEREASTRA ===================== -->
    <Storyboard x:Key="FadeInWindowStoryboard">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0" To="1" Duration="0:0:0.30">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <!-- ===================== GLASS CARD (Border) ===================== -->
    <Style x:Key="GlassCard" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource BgCardBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSoftBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="16"/>
        <Setter Property="Padding" Value="30"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#FF000000" BlurRadius="32" ShadowDepth="8" Direction="270" Opacity="0.55"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== BUTON MODERN (glow + scale la hover) ===================== -->
    <Style x:Key="ModernButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource AccentGradientBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="18,10"/>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root" CornerRadius="10" Background="{TemplateBinding Background}">
                        <Border.Effect>
                            <DropShadowEffect x:Name="Glow" Color="{StaticResource ColorAccent}" BlurRadius="0" ShadowDepth="0" Opacity="0.0"/>
                        </Border.Effect>
                        <Border.RenderTransform>
                            <ScaleTransform x:Name="Scale" ScaleX="1" ScaleY="1"/>
                        </Border.RenderTransform>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Glow" Storyboard.TargetProperty="BlurRadius" To="22" Duration="0:0:0.18"/>
                                        <DoubleAnimation Storyboard.TargetName="Glow" Storyboard.TargetProperty="Opacity" To="0.85" Duration="0:0:0.18"/>
                                        <DoubleAnimation Storyboard.TargetName="Scale" Storyboard.TargetProperty="ScaleX" To="1.03" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="Scale" Storyboard.TargetProperty="ScaleY" To="1.03" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Glow" Storyboard.TargetProperty="BlurRadius" To="0" Duration="0:0:0.18"/>
                                        <DoubleAnimation Storyboard.TargetName="Glow" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.18"/>
                                        <DoubleAnimation Storyboard.TargetName="Scale" Storyboard.TargetProperty="ScaleX" To="1" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="Scale" Storyboard.TargetProperty="ScaleY" To="1" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Scale" Property="ScaleX" Value="0.97"/>
                            <Setter TargetName="Scale" Property="ScaleY" Value="0.97"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
        <Setter Property="Background" Value="#FF21262D"/>
    </Style>

    <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
        <Setter Property="Background" Value="{StaticResource ColorDanger}"/>
    </Style>

    <!-- ===================== TEXTBOX MODERN (glow la focus) ===================== -->
    <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="#FF0D1117"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="CaretBrush" Value="{StaticResource AccentLightBrush}"/>
        <Setter Property="BorderThickness" Value="1.5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="Border" CornerRadius="8" Background="{TemplateBinding Background}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Border.BorderBrush>
                            <SolidColorBrush x:Name="DynamicBorder" Color="{StaticResource ColorBorder}"/>
                        </Border.BorderBrush>
                        <Border.Effect>
                            <DropShadowEffect x:Name="FocusGlow" Color="{StaticResource ColorAccent}" BlurRadius="0" ShadowDepth="0" Opacity="0"/>
                        </Border.Effect>
                        <ScrollViewer x:Name="PART_ContentHost" Margin="0" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="DynamicBorder" Storyboard.TargetProperty="Color" To="{StaticResource ColorAccent}" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="FocusGlow" Storyboard.TargetProperty="BlurRadius" To="14" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="FocusGlow" Storyboard.TargetProperty="Opacity" To="0.6" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="DynamicBorder" Storyboard.TargetProperty="Color" To="{StaticResource ColorBorder}" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="FocusGlow" Storyboard.TargetProperty="BlurRadius" To="0" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="FocusGlow" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
