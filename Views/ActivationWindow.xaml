<Window x:Class="LogAnalyzer.UI.Views.ActivationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Activare Licență LogAnalyzer Enterprise" Height="460" Width="600"
        WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent" Foreground="#f8fafc">

    <Window.Resources>
        <SolidColorBrush x:Key="BgBrush" Color="#070812"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#0c0f1c"/>
        <SolidColorBrush x:Key="Surface2Brush" Color="#111528"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#1e2538"/>
        <SolidColorBrush x:Key="PrimaryAccentBrush" Color="#8b5cf6"/>
        <SolidColorBrush x:Key="CyberBlueBrush" Color="#38bdf8"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#f5f7ff"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#a7adc2"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#ef4444"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#22c55e"/>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#0c1020"/>
            <Setter Property="Foreground" Value="#f8fafc"/>
            <Setter Property="BorderBrush" Value="#22304c"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="MinHeight" Value="38"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="#a78bfa"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Focusable="False" 
                                          VerticalAlignment="Center" 
                                          HorizontalAlignment="Stretch"
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#8b5cf6"/>
                                <Setter TargetName="border" Property="Background" Value="#11172e"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BgBrush}" BorderBrush="{StaticResource PrimaryAccentBrush}" BorderThickness="1.5" CornerRadius="10" Padding="32">
        <Border.Effect>
            <DropShadowEffect Color="#8b5cf6" Direction="270" ShadowDepth="0" Opacity="0.35" BlurRadius="20"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Brand Header -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
                <TextBlock Text="🛡️ LOGANALYZER ENTERPRISE" FontSize="20" FontWeight="Black" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="Nod de Comandă Neînregistrat — Introduceți Licența" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource DangerBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Inputs -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <TextBlock Text="IDENTIFICATOR HARDWARE UNIC (HWID):" FontSize="9.5" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" Margin="2,0,0,6"/>
                <TextBox x:Name="TxtHardwareId" Height="40" Foreground="{StaticResource CyberBlueBrush}" IsReadOnly="True" TextAlignment="Center" Margin="0,0,0,16"/>

                <TextBlock Text="INTRODUCEȚI CHEIA DE ACTIVARE (20 CARACTERE):" FontSize="9.5" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" Margin="2,0,0,6"/>
                <TextBox x:Name="TxtLicenseKey" Height="40" BorderBrush="{StaticResource PrimaryAccentBrush}" TextAlignment="Center" Margin="0,0,0,16"/>
            </StackPanel>

            <!-- Actions -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="⚡ Activează Licența" Width="180" Height="38" Background="{StaticResource PrimaryAccentBrush}" Foreground="White" FontWeight="Bold" FontSize="11.5" BorderThickness="0" Click="Activate_Click" Margin="0,0,12,0" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="5">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="btnBorder" Property="Background" Value="#7c3aed"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Content="Închide" Width="110" Height="38" Background="{StaticResource Surface2Brush}" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="SemiBold" FontSize="11.5" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Click="Exit_Click" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="btnBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="btnBorder" Property="Background" Value="#1a2035"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>