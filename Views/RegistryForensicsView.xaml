<UserControl x:Class="LogAnalyzer.UI.Views.RegistryForensicsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Search Bar -->
        <Border Grid.Row="0" Style="{StaticResource ObsidianCardStyle}" Margin="0,0,0,12" Padding="14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding SearchArtifactsText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                <Button Grid.Column="1" Content="Clear Filter" Command="{Binding ClearRegistryFilterCommand}" Width="100"/>
            </Grid>
        </Border>

        <!-- Main Registry DataGrid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding RegistryArtifacts}" SelectedItem="{Binding SelectedArtifact}" AutoGenerateColumns="False" IsReadOnly="True" EnableRowVirtualization="True">
            <DataGrid.ContextMenu>
                <ContextMenu Background="{StaticResource Surface2Brush}" BorderBrush="{StaticResource BorderBrush}">
                    <MenuItem Header="Inspect Registry Key Details" Command="{Binding OpenGenericDetailCommand}" CommandParameter="{Binding SelectedArtifact}"/>
                    <Separator Background="{StaticResource BorderBrush}"/>
                    <MenuItem Header="Filter by Category" Command="{Binding PivotRegistryCategoryCommand}" CommandParameter="{Binding SelectedArtifact}"/>
                    <MenuItem Header="Filter by Key Path" Command="{Binding PivotRegistryKeyPathCommand}" CommandParameter="{Binding SelectedArtifact}"/>
                    <Separator Background="{StaticResource BorderBrush}"/>
                    <MenuItem Header="Copy Value Data" Command="{Binding CopyRegistryValueDataCommand}" CommandParameter="{Binding SelectedArtifact}"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="140"/>
                <DataGridTextColumn Header="Key Path" Binding="{Binding KeyPath}" Width="280"/>
                <DataGridTextColumn Header="Value Name" Binding="{Binding ValueName}" Width="160"/>
                <DataGridTextColumn Header="Value Data" Binding="{Binding ValueData}" Width="*"/>
            </DataGrid.Columns>
            <DataGrid.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding OpenGenericDetailCommand}" CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
            </DataGrid.InputBindings>
        </DataGrid>

        <!-- Pagination -->
        <Grid Grid.Row="2" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Registry Hives: 100 entries / page" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="◀ Prev" Command="{Binding PrevRegistryPageCommand}" Width="80" Margin="2,0"/>
                <TextBlock Text="{Binding RegistryCurrentPage}" VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}" Margin="8,0" FontWeight="Bold"/>
                <TextBlock Text=" / " VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBlock Text="{Binding RegistryTotalPages}" VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,8,0"/>
                <Button Content="Next ▶" Command="{Binding NextRegistryPageCommand}" Width="80" Margin="2,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>